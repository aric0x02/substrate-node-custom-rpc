version: "3.2"

services:
  dev:
    container_name: move-contracts-node
    image: paritytech/ci-linux:production
    working_dir: /var/www/move-contracts-node
    ports:
      - "9944:9944"
    environment:
      - CARGO_HOME=/var/www/move-contracts-node/.cargo
    volumes:
      - .:/var/www/move-contracts-node
      - type: bind
        source: ./.local
        target: /root/.local
    command: bash -c "cargo build --release && ./target/release/move-contracts-node --dev --ws-external"
